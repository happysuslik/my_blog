<%= simple_form_for @user do |f| %>
	<%= f.input :email %>
  <%= f.input :password %>
  <%= f.input :confirm_password %>
  <%= f.button :submit %>
  <%= link_to('Back', users_path, class: "btn btn-default") %>
<% end %>